// ======================================================================
// \title  DpRecordSerializableAc.cpp
// \author Generated by fpp-to-cpp
// \brief  cpp file for DpRecord struct
// ======================================================================

#include "F-Prime/Svc/DpCatalog/DpRecordSerializableAc.hpp"
#include "Fw/Types/Assert.hpp"

namespace Svc {

  // ----------------------------------------------------------------------
  // Constructors
  // ----------------------------------------------------------------------

  DpRecord ::
    DpRecord() :
      Serializable(),
      m_id(0),
      m_tSec(0),
      m_tSub(0),
      m_priority(0),
      m_size(0),
      m_state(Fw::DpState::UNTRANSMITTED)
  {

  }

  DpRecord ::
    DpRecord(
        U32 id,
        U32 tSec,
        U32 tSub,
        U32 priority,
        U64 size,
        Fw::DpState::T state
    ) :
      Serializable(),
      m_id(id),
      m_tSec(tSec),
      m_tSub(tSub),
      m_priority(priority),
      m_size(size),
      m_state(state)
  {

  }

  DpRecord ::
    DpRecord(const DpRecord& obj) :
      Serializable(),
      m_id(obj.m_id),
      m_tSec(obj.m_tSec),
      m_tSub(obj.m_tSub),
      m_priority(obj.m_priority),
      m_size(obj.m_size),
      m_state(obj.m_state)
  {

  }

  // ----------------------------------------------------------------------
  // Operators
  // ----------------------------------------------------------------------

  DpRecord& DpRecord ::
    operator=(const DpRecord& obj)
  {
    if (this == &obj) {
      return *this;
    }

    set(obj.m_id, obj.m_tSec, obj.m_tSub, obj.m_priority, obj.m_size, obj.m_state);
    return *this;
  }

  bool DpRecord ::
    operator==(const DpRecord& obj) const
  {
    return (
      (this->m_id == obj.m_id) &&
      (this->m_tSec == obj.m_tSec) &&
      (this->m_tSub == obj.m_tSub) &&
      (this->m_priority == obj.m_priority) &&
      (this->m_size == obj.m_size) &&
      (this->m_state == obj.m_state)
    );
  }

  bool DpRecord ::
    operator!=(const DpRecord& obj) const
  {
    return !(*this == obj);
  }

#ifdef BUILD_UT

  std::ostream& operator<<(std::ostream& os, const DpRecord& obj) {
    Fw::String s;
    obj.toString(s);
    os << s.toChar();
    return os;
  }

#endif

  // ----------------------------------------------------------------------
  // Member functions
  // ----------------------------------------------------------------------

  Fw::SerializeStatus DpRecord ::
    serialize(Fw::SerializeBufferBase& buffer) const
  {
    Fw::SerializeStatus status;

    status = buffer.serialize(this->m_id);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
    status = buffer.serialize(this->m_tSec);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
    status = buffer.serialize(this->m_tSub);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
    status = buffer.serialize(this->m_priority);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
    status = buffer.serialize(this->m_size);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
    status = buffer.serialize(this->m_state);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }

    return status;
  }

  Fw::SerializeStatus DpRecord ::
    deserialize(Fw::SerializeBufferBase& buffer)
  {
    Fw::SerializeStatus status;

    status = buffer.deserialize(this->m_id);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
    status = buffer.deserialize(this->m_tSec);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
    status = buffer.deserialize(this->m_tSub);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
    status = buffer.deserialize(this->m_priority);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
    status = buffer.deserialize(this->m_size);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
    status = buffer.deserialize(this->m_state);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }

    return status;
  }

#if FW_SERIALIZABLE_TO_STRING

  void DpRecord ::
    toString(Fw::StringBase& sb) const
  {
    static const char* formatString =
      "( "
      "id = %" PRIu32 ", "
      "tSec = %" PRIu32 ", "
      "tSub = %" PRIu32 ", "
      "priority = %" PRIu32 ", "
      "size = %" PRIu64 ", "
      "state = %s"
      " )";

    // Declare strings to hold any serializable toString() arguments
    Fw::String stateStr;

    // Call toString for arrays and serializable types
    this->m_state.toString(stateStr);

    sb.format(
      formatString,
      this->m_id,
      this->m_tSec,
      this->m_tSub,
      this->m_priority,
      this->m_size,
      stateStr.toChar()
    );
  }

#endif

  // ----------------------------------------------------------------------
  // Setter functions
  // ----------------------------------------------------------------------

  void DpRecord ::
    set(
        U32 id,
        U32 tSec,
        U32 tSub,
        U32 priority,
        U64 size,
        Fw::DpState::T state
    )
  {
    this->m_id = id;
    this->m_tSec = tSec;
    this->m_tSub = tSub;
    this->m_priority = priority;
    this->m_size = size;
    this->m_state = state;
  }

  void DpRecord ::
    setid(U32 id)
  {
    this->m_id = id;
  }

  void DpRecord ::
    settSec(U32 tSec)
  {
    this->m_tSec = tSec;
  }

  void DpRecord ::
    settSub(U32 tSub)
  {
    this->m_tSub = tSub;
  }

  void DpRecord ::
    setpriority(U32 priority)
  {
    this->m_priority = priority;
  }

  void DpRecord ::
    setsize(U64 size)
  {
    this->m_size = size;
  }

  void DpRecord ::
    setstate(Fw::DpState::T state)
  {
    this->m_state = state;
  }

}
